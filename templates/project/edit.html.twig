{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center mt-5 mb-3">Edit Project</h1>
<div class="card">
    <div class="card-header">
    {{ include('project/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-info" href="{{ path('app_project_index') }}">back to list</a>

    {{ include('project/_delete_form.html.twig') }}
    </div>
</div>
</div>
{% endblock %}
